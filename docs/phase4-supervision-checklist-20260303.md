# Phase 4 监督检查清单（2026-03-03）

负责人：Codex
目标：在用户体验简单化的前提下，构建可独立运行的工作场景 + 自主扩展底层。

## 1. 全局架构检查

- [x] 工作场景前端信息架构落地（左配置/中回执/右工具/底输入）
- [x] Workbench 后端状态与动作 API 落地
- [x] 扩展内核 API（状态/安装/下载）落地
- [x] 前后端类型契约已对齐（desktop api.ts + mock-server）
- [x] 构建与脚本语法检查通过

## 2. 角色分工检查（防重复）

### Codex（产品+架构+工程）
- [x] 负责 API 契约与状态机
- [x] 负责后端扩展内核与治理策略
- [x] 负责集成验收与回归

### Aria Kernel（底层能力贡献）
- [ ] 产出 pack manifest 规范（版本/依赖/回滚/权限）
- [ ] 产出工具路由 fallback 规范
- [ ] 产出离线可运行能力包样例

### UI 同事（展示层）
- [x] 样式层支持工作场景白板布局
- [ ] 优化工作台移动端交互密度
- [ ] 优化工具回执视觉层级（成功/失败/重试）

## 3. 用户视角检查（小白）

- [x] 是否看得懂：模块分区清晰
- [x] 是否会使用：输入一句话即可触发工作计划
- [x] 是否有结果：执行后有回执
- [x] 是否能扩展：可安装扩展包、可触发自主下载
- [x] 是否足够安全：域名/文件类型/大小白名单已落地（可继续增强签名机制）

## 4. 风险与补强（下一轮）

1. 下载能力继续增强：
   - 增加 denylist、签名校验和风险评分策略
2. 扩展安装需要签名校验：
   - manifest signature + trust source
3. 多命令动作需要事务边界：
   - 失败回滚 + 幂等 key
4. 审计查询需要独立面板：
   - 便于用户一眼看懂“做了什么”

## 5. 本轮验收证据

1. `node --check services/api/mock-server.mjs` 通过
2. `npm run build:web` 通过
3. 实测新接口链路：
   - `/v1/workbench/state`
   - `/v1/workbench/intent`
   - `/v1/expansion/packs/install`
   - `/v1/expansion/fetch-download`
