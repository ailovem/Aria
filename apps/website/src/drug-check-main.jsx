import React from 'react';
import ReactDOM from 'react-dom/client';
import DrugCheckPage from './drug-check/DrugCheckPage.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <DrugCheckPage />
    </React.StrictMode>,
);
